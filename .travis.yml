language: php

php: 
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - 7.2

env:
  - LV="4.1.*"
  - LV="4.2.*"
  - LV="5.1.*"
  - LV="5.5.*"
  - LV="5.6.*"

before_install:
  - composer require laravel/framework:${LV} laravel/laravel:${LV} --no-update

install:
  - composer install --no-interaction

script:
  - phpunit

sudo: false
